[project]
name = "private-incidents-mcp"
version = "0.1.0"
description = "Mock private FSI incidents MCP server for OpenClaw VNet demos."
requires-python = ">=3.11,<3.14"
dependencies = [
    "fastmcp",
    "PyJWT",
    "uvicorn",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
