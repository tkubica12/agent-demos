# Backend and Frontend Configuration
# This .env file is for the backend API and frontend web app only
# Teams agent has its own .env file in teams-agent/.env

# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://YOUR-RESOURCE-NAME.openai.azure.com
AZURE_OPENAI_MODEL_DEPLOYMENT=gpt-4o

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_BASE_URL=http://localhost:8000

# Note: No API key needed - using EntraID authentication with DefaultAzureCredential
# Make sure you are logged in with Azure CLI: az login
